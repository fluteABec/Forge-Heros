<form method="post" action="{{ path('app_character_class_delete', {'id': character_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ character_class.id) }}">
    <button class="btn">Delete</button>
</form>

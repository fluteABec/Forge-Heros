{{ form_start(form, {
    attr: {
        class: 'app-form',
        id: 'character-form',
        'data-controller': 'point-buy',
        'data-point-buy-budget-value': '27',
        'data-point-buy-base-value': '8'
    }
}) }}

<div class="point-buy-box">
    <div>
        <p class="eyebrow">Repartition des points</p>
        <p class="point-buy-text">
            Points restants : <strong id="points-remaining" data-point-buy-target="remaining">27</strong> / 27
        </p>
    </div>
    <p class="stat-label" id="points-status" data-point-buy-target="status">
        Chaque caracteristique part de 8 et coute 1 point par niveau supplementaire.
    </p>
</div>

{{ form_widget(form) }}
<button class="button">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
